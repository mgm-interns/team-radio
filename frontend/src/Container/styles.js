export default ({ spacing }) => ({
  container: {
    width: spacing.fullWidth,
    minHeight: spacing.fullViewportHeight,
    marginLeft: 'auto',
    marginRight: 'auto',
    position: 'relative',
  },
});
