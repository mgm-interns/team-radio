export default ({ spacing }) => ({
  containerWrapper: {
    width: spacing.fullWidth,
    margin: 0,
    paddingBottom: 50, // Footer height
  },
  progress: {
    // margin: 'auto',
    // display: 'flex',
    // justifyContent: 'center',
    // alignItems: 'center',
  },
});
