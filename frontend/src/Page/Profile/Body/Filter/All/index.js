import React, { Component } from 'react';
import PropTypes from 'prop-types';

import Typography from 'material-ui/Typography';

const FilterAll = () => <Typography type="subheading">Recent</Typography>;

export default FilterAll;
