export default ({ spacing }) => ({
  container: {
    height: '100%',
  },
  emptyContainer: {
    backgroundColor: 'rgba(0,0,0,0.0225)',
    height: '100%',
  },
  emptyIcon: {
    fontSize: 56,
    marginBottom: spacing.baseMargin,
  },
});
